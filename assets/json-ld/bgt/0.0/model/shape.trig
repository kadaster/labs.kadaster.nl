prefix absence-reason: <https://bgt.basisregistraties.overheid.nl/bgt/id/absense-reason/>
prefix bag: <http://bag.basisregistraties.overheid.nl/def/bag#>
prefix bag-pand: <http://bag.basisregistraties.overheid.nl/bag/id/pand/>
prefix begroeid-terreindeel: <https://bgt.basisregistraties.overheid.nl/bgt/id/begroeid-terreindeel/>
prefix bgt: <https://bgt.basisregistraties.overheid.nl/bgt/def/>
prefix bronhouder: <https://bgt.basisregistraties.overheid.nl/bgt/id/bronhouder/>
prefix brt: <http://brt.basisregistraties.overheid.nl/def/top10nl#>
prefix cbs-gemeente: <https://data.labs.kadaster.nl/cbs/wbk/id/gemeente/>
prefix collectie: <https://bgt.basisregistraties.overheid.nl/bgt/id/collectie/>
prefix dcat: <http://www.w3.org/ns/dcat#>
prefix dcm: <http://purl.org/dc/dcmitype/>
prefix dct: <http://purl.org/dc/terms/>
prefix dqv: <http://www.w3.org/ns/dqv#>
prefix entailment: <http://www.w3.org/ns/entailment/>
prefix fmt: <http://www.w3.org/ns/formats/>
prefix foaf: <http://xmlns.com/foaf/0.1/>
prefix fysiek-voorkomen: <https://bgt.basisregistraties.overheid.nl/bgt/id/fysiek-voorkomen/>
prefix geo: <http://www.opengis.net/ont/geosparql#>
prefix gml: <http://www.opengis.net/ont/gml#>
prefix graph: <https://bgt.basisregistraties.overheid.nl/bgt/id/graph/>
prefix img: <https://bgt.basisregistraties.overheid.nl/bgt/id/img/>
prefix inwinningsmethode: <https://bgt.basisregistraties.overheid.nl/bgt/id/inwinningsmethode/>
prefix ldqd: <http://www.w3.org/2016/05/ldqd#>
prefix nen3610: <http://definities.geostandaarden.nl/def/nen3610#>
prefix oa: <http://www.w3.org/ns/oa#>
prefix onbegroeid-terreindeel: <https://bgt.basisregistraties.overheid.nl/bgt/id/onbegroeid-terreindeel/>
prefix owl: <http://www.w3.org/2002/07/owl#>
prefix pand: <https://bgt.basisregistraties.overheid.nl/bgt/id/pand/>
prefix plaatsbepalingspunt: <https://bgt.basisregistraties.overheid.nl/bgt/id/plaatsbepalingspunt/>
prefix profile: <http://www.w3.org/ns/owl-profile/>
prefix prov: <http://www.w3.org/ns/prov#>
prefix quality-annotation: <https://data.label.kadaster.nl/kadaster/bgt/id/quality-annotation/>
prefix quality-dimension: <https://bgt.basisregistraties.overheid.nl/bgt/concept/quality-dimension/>
prefix rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#>
prefix registratief-gebied: <http://brt.basisregistraties.overheid.nl/top10nl/id/registratief-gebied/>
prefix sd: <http://www.w3.org/ns/sparql-service-description#>
prefix sdo: <https://schema.org/>
prefix sh: <http://www.w3.org/ns/shacl#>
prefix shape: <https://bgt.basisregistraties.overheid.nl/bgt/id/shape/>
prefix skos: <http://www.w3.org/2004/02/skos/core#>
prefix status: <https://bgt.basisregistraties.overheid.nl/bgt/id/status/>
prefix stelselcatalogus: <https://opendata.stelselcatalogus.nl/stelselcatalogus/def#>
prefix tbd: <https://example.com/>
prefix value: <https://triplydb.com/Triply/value/>
prefix vann: <http://purl.org/vocab/vann/>
prefix void: <http://rdfs.org/ns/void#>
prefix wet: <https://bgt.basisregistraties.overheid.nl/bgt/id/wet/>
prefix xsd: <http://www.w3.org/2001/XMLSchema#>

graph:shape {
  shape:BakRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:geometrie,
      shape:plusType,
      shape:type;
    sh:targetClass bgt:BakRegistratie.

  shape:BegroeidTerreindeelRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:fysiekVoorkomen,
      shape:geometrie,
      shape:kruinlijn,
      shape:plusFysiekVoorkomen,
      shape:talud;
    sh:targetClass bgt:BegroeidTerreindeelRegistratie.

  shape:BordRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:geometrie,
      shape:plusType,
      shape:type;
    sh:targetClass bgt:BordRegistratie.

  shape:BuurtRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:buurtcode,
      shape:wijk;
    sh:targetClass bgt:BuurtRegistratie.

  shape:FunctioneelGebiedRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:geometrie,
      shape:naam,
      shape:plusType,
      shape:type;
    sh:targetClass bgt:FunctioneelGebiedRegistratie.

  shape:GebouwinstallatieRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:geometrie,
      shape:plusType,
      shape:type;
    sh:targetClass bgt:GebouwinstallatieRegistratie.

  shape:Identificatie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    rdfs:seeAlso "https://docs.geostandaarden.nl/imgeo/catalogus/bgt/#nen3610id"^^xsd:anyURI;
    sh:name "Identificatie"@nl;
    sh:property
      shape:lokaalID,
      shape:namespace;
    sh:targetClass nen3610:Identificatie;
    skos:definition
      [ dct:creator "BGT";
        rdf:value "Identificatiegegevens voor de universeel unieke identificatie van een object."@nl ];
    skos:scopeNote "De combinatie van namespace van een registratie, lokale identificatie en versie informatie maken een object uniek identificeerbaar.  Met het NEN3610ID kun je daardoor met zekerheid verwijzen naar het geïdentificeerde object."@nl.

  shape:IMGeoObject
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property shape:registratie;
    sh:targetClass bgt:IMGeoObject.

  shape:IMGeoObjectRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:eindRegistratie,
      shape:identificatie,
      shape:inOnderzoek,
      shape:object,
      shape:objectBeginTijd,
      shape:objectEindTijd,
      shape:publicatiedatumLandelijkeVoorzieningen,
      shape:relatieveHoogteligging,
      shape:status,
      shape:tijdstipRegistratie;
    sh:targetClass bgt:IMGeoObjectRegistratie.

  shape:InrichtingselementRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property shape:type;
    sh:targetClass bgt:InrichtingselementRegistratie.

  shape:InstallatieRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:geometrie,
      shape:plusType,
      shape:type;
    sh:targetClass bgt:InstallatieRegistratie.

  shape:KastRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:geometrie,
      shape:plusType,
      shape:type;
    sh:targetClass bgt:KastRegistratie.

  shape:MastRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:geometrie,
      shape:plusType,
      shape:type;
    sh:targetClass bgt:MastRegistratie.

  shape:KunstwerkdeelRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:plusType,
      shape:type;
    sh:targetClass bgt:KunstwerkdeelRegistratie.

  shape:Nummeraanduidingreeks
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      # nummeraanduidingreeks/identificatieBAGVBOLaagsteHuisnummer
      shape:bagEersteVerblijfsobject,
      # nummeraanduidingreeks/identificatieBAGVBOHoogsteHuisnummer
      shape:bagLaatsteVerblijfsobject;
    sh:targetClass bgt:Nummeraanduidingreeks.

  shape:OnbegroeidTerreindeelRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:fyiekVoorkomen,
      shape:geometrie,
      shape:kruinlijn,
      shape:plusFysiekVoorkomen,
      shape:talud;
    sh:targetClass bgt:OnbegroeidTerreindeelRegistratie.

  shape:OndersteunendWaterdeelRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:geometrie,
      shape:plusType,
      shape:type;
    sh:targetClass bgt:OndersteunendWaterdeelRegistratie.

  shape:OndersteunendWegdeelRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:functie,
      shape:fysiekVoorkomen,
      shape:geometrie,
      shape:kruinlijn,
      shape:plusFunctie,
      shape:plusFysiekvoorkomen,
      shape:talud;
    sh:targetClass bgt:OndersteunendWegdeelRegistratie.

  shape:OpenbareRuimteRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property shape:naamEnIdOpenbareRuimte;
    sh:targetClass bgt:OpenbareRuimteRegistratie.

  shape:OpenbareRuimteLabelRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:identificatieBagOpenbareRuimte,
      shape:openbareRuimteNaam,
      shape:openbareRuimteType;
    sh:targetClass bgt:OpenbareRuimteLabelRegistratie.

  shape:OverbruggingsdeelRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:beweegbaar,
      shape:geometrie,
      shape:hoortBijTypeOverbrugging,
      shape:typeOverbruggingsdeel;
    sh:targetClass bgt:OverbruggingsdeelRegistratie.

  shape:OverigBouwwerkRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:plusType,
      shape:type;
    sh:targetClass bgt:OverigBouwwerkRegistratie.

  shape:OverigeConstructieRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property shape:geometrie;
    sh:targetClass bgt:OverigeConstructieRegistratie.

  shape:PaalRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:geometrie,
      shape:hectometeraanduiding,
      shape:plusType,
      shape:type;
    sh:targetClass bgt:PaalRegistratie.

  shape:PandRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:geometrie,
      shape:nummeraanduidingreeks,
      shape:identificatieBagPand;
    sh:targetClass bgt:PandRegistratie.

  shape:PlaatsbepalingspuntRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:datumInwinning,
      shape:geometrie,
      shape:identificatie,
      shape:inwinnendeInstantie,
      shape:inwinningsmethode,
      shape:nauwkeurigheid;
    sh:targetClass bgt:PlaatsbepalingspuntRegistratie.

  shape:PutRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:geometrie,
      shape:plusType,
      shape:type;
    sh:targetClass bgt:PutRegistratie.

  shape:RegistratiefGebiedRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:geometrie,
      shape:naam;
    sh:targetClass bgt:RegistratiefGebiedRegistratie.

  shape:ScheidingRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:plusType,
      shape:type;
    sh:targetClass bgt:ScheidingRegistratie.

  shape:SensorRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:geometrie,
      shape:plusType,
      shape:type;
    sh:targetClass bgt:SensorRegistratie.

  shape:SpoorRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:functie,
      shape:geometrie,
      shape:plusFunctie;
    sh:targetClass bgt:SpoorRegistratie.

  shape:StadsdeelRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:targetClass bgt:StadsdeelRegistratie.

  shape:StraatmeubilairRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:geometrie,
      shape:plusType,
      shape:type;
    sh:targetClass bgt:StraatmeubilairRegistratie.

  shape:TunneldeelRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property shape:geometrie;
    sh:targetClass bgt:TunneldeelRegistratie.

  shape:VegetatieObjectRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:geometrie,
      shape:plusType,
      shape:type;
    sh:targetClass bgt:VegetatieObjectRegistratie.

  shape:WaterinrichtingselementRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:geometrie,
      shape:plusType,
      shape:type;
    sh:targetClass bgt:WaterinrichtingselementRegistratie.

  shape:WaterdeelRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:geometrie,
      shape:plusType,
      shape:type;
    sh:targetClass bgt:WaterdeelRegistratie.

  shape:WaterschapRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:targetClass bgt:WaterschapRegistratie.

  shape:WegdeelRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:functie,
      shape:fysiekVoorkomen,
      shape:geometrie,
      shape:kruinlijn,
      shape:plusFunctie,
      shape:plusFysiekVoorkomen,
      shape:talud;
    sh:targetClass bgt:WegdeelRegistratie.

  shape:WeginrichtingselementRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:geometrie,
      shape:plusType,
      shape:type;
    sh:targetClass bgt:WeginrichtingselementRegistratie.

  shape:WijkRegistratie
    a sh:NodeShape;
    rdfs:isDefinedBy graph:shape;
    sh:property
      shape:stadsdeel,
      shape:wijkcode;
    sh:targetClass bgt:WijkRegistratie.

  shape:beweegbaar
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:datatype xsd:boolean;
    sh:maxCount 1;
    sh:minCount 0;
    sh:path bgt:beweegbaar.

  shape:bronhouder
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:class bgt:Bronhouder;
    sh:maxCount 1;
    sh:minCount 1;
    sh:path bgt:bronhouder.

  shape:buurtcode
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:datatype xsd:string;
    sh:maxCount 1;
    sh:minCount 1;
    sh:path bgt:buurtcode.

  shape:datumInwinning
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:datatype xsd:date;
    sh:maxCount 1;
    sh:minCount 1;
    sh:path bgt:datumInwinning.

  shape:eindRegistratie
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    rdfs:seeAlso
      "https://docs.geostandaarden.nl/imgeo/catalogus/bgt/#eindregistratie"^^xsd:anyURI,
      "https://docs.geostandaarden.nl/nen3610/nldp/#regels-attributen-formeleHistorie"^^xsd:anyURI;
    sh:datatype xsd:dateTime;
    sh:maxCount 1;
    sh:minCount 0;
    sh:name "Heeft eind registratie"@nl;
    sh:path prov:invalidatedAtTime;
    skos:definition
      [ dct:creator "BGT";
        rdf:value "Eind van de periode waarop deze instantie van het object geldig is bij de bronhouder. Wanneer deze waarde niet is ingevuld is de instantie nog geldig."@nl ].

  shape:functie
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:class owl:Class;
    sh:maxCount 1;
    sh:minCount 1;
    sh:path bgt:functie.

  shape:fysiekVoorkomen
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:class bgt:FysiekVoorkomen;
    sh:maxCount 1;
    sh:minCount 1;
    sh:path bgt:fysiekVoorkomen.

  #<section lang="nl">
  #  <p>De linked data BGT gebruikt GeoSPARQL voor het representeren van de geometrieën.  Een generieke geometrie in de BGT wordt aangeduid met een curve (klasse <code>gml:Curve</code>), een multi-vlak (klasse <code>gml:MultiSurface</code>), een punt (klasse <code>gml:Point</code> of een vlak (klasse <code>gml:Surface</code>).</p>
  #</section>"""^^rdf:HTML
  shape:geometrie
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    # gml:Curve,
    # gml:MultiSurface,
    # gml:Point,
    # gml:Surface;
    sh:datatype geo:wktLiteral;
    sh:maxCount 1;
    sh:minCount 1;
    sh:name "Heeft tweedimensionale punt-, lijn-, vlak- of multivlakgeometrie"@nl;
    sh:path bgt:geometrie.

  shape:hectometeraanduiding
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:datatype xsd:string;
    sh:maxCount 1;
    sh:minCount 0;
    sh:path bgt:hectometeraanduiding.

  shape:hoek
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:datatype xsd:double;
    sh:maxCount 1;
    sh:minCount 1;
    sh:path bgt:hoek.

  shape:hoortBijTypeOverbrugging
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:class
      [ a owl:Class;
        owl:oneOf
          ( bgt:Aquaduct
            bgt:Brug
            bgt:Ecoduct
            bgt:Flyover
            bgt:Viaduct ) ];
    sh:maxCount 1;
    sh:minCount 0;
    sh:path bgt:hoortBijTypeOverbrugging.

  shape:identificatie
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    rdfs:seeAlso "https://docs.geostandaarden.nl/imgeo/catalogus/bgt/#identificatie-0"^^xsd:anyURI;
    sh:class nen3610:Identificatie;
    sh:description "Uniek identificatienummer voor het object dat onveranderlijk is zolang het object bestaat."@nl;
    sh:maxCount 1;
    sh:minCount 1;
    sh:name "Heeft identificatie"@nl;
    sh:path nen3610:identificatie.

  shape:identificatieBagOpenbareRuimte
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    rdfs:seeAlso "https://docs.geostandaarden.nl/imgeo/catalogus/bgt/#identificatiebagopr"^^xsd:anyURI;
    sh:class bag:OpenbareRuimte;
    sh:maxCount 1;
    sh:minCount 1;
    sh:name "Heeft BAG openbare ruimte"@nl;
    sh:path bgt:bagOpenbareRuimte;
    skos:definition
      [ dct:creator "BAG";
        rdf:value "De unieke identificatie van het object zoals is toegekend in de BAG-administratie."@nl ].

  shape:identificatieBagPand
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:class bag:Pand;
    sh:maxCount 1;
    sh:minCount 1;
    sh:path bgt:bagPand.

  shape:inOnderzoek
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    rdfs:seeAlso "https://docs.geostandaarden.nl/imgeo/catalogus/bgt/#inonderzoek"^^xsd:anyURI;
    sh:datatype xsd:boolean;
    sh:maxCount 1;
    sh:minCount 1;
    sh:path bgt:inOnderzoek.

  shape:inwinnendeInstantie
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    rdfs:seeAlso "https://docs.geostandaarden.nl/imgeo/catalogus/bgt/#inwinnendeinstantie"^^xsd:anyURI;
    sh:class tbd:;
    sh:maxCount 1;
    sh:minCount 1;
    sh:path bgt:inwinnendeInstantie.

  shape:inwinningsmethode
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    rdfs:seeAlso "https://docs.geostandaarden.nl/imgeo/catalogus/bgt/#inwinningsmethode"^^xsd:anyURI;
    sh:class bgt:InwinningsMethode;
    sh:maxCount 1;
    sh:minCount 1;
    sh:path bgt:inwinningsmethode.

  shape:kruinlijn
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:datatype geo:wktLiteral;
    sh:maxCount 1;
    sh:minCount 0;
    sh:path bgt:kruinlijn.

  shape:lokaalID
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    rdfs:seeAlso "https://docs.geostandaarden.nl/imgeo/catalogus/bgt/#lokaalid"^^xsd:anyURI;
    sh:datatype xsd:string;
    sh:maxCount 1;
    sh:minCount 1;
    sh:name "Lokaal ID"@nl;
    sh:path nen3610:lokaalID;
    skos:definition
      [ dct:creator "BGT";
        rdf:value "Unieke identificatiecode binnen een registratie."@nl ];
    skos:scopeNote "LokaalID is de identificatiecode die een object heeft binnen een (lokaal) registratie. De volgende karakters mogen in een lokaalID voorkomen: {A…Z, a…z, 0…9, “_”, “-“, “,”, ”.”}  Dit is de IMGeo identificatie. Een betekenisloos nummer."@nl.

  shape:naam
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:datatype rdf:langString;
    sh:maxCount 1;
    sh:minCount 0;
    sh:path bgt:naam.

  shape:namespace
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    rdfs:seeAlso "https://docs.geostandaarden.nl/imgeo/catalogus/bgt/#namespace"^^xsd:anyURI;
    sh:datatype xsd:string;
    sh:maxCount 1;
    sh:minCount 1;
    sh:name "Namespace"@nl;
    sh:path nen3610:namespace;
    skos:definition
      [ dct:creator "BGT";
        rdf:value "Unieke verwijzing naar een registratie van objecten."@nl ];
    skos:scopeNote "Het attribuut ‘namespace’ is een unieke verwijzing naar de registratie die de identificatie uitdeelt. Deze lijst van registraties wordt beheerd door de beheerder van NEN3610. Binnen Nederland zal deze namespace vrijwel altijd met ‘NL.’ beginnen. De volgende karakters mogen in een namespace aanduiding voorkomen: {”A” …”Z”, “a”…”z”,”0”…”9”, “_”, “-“, “,”,”.”}"@nl.

  shape:nauwkeurigheid
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:datatype xsd:integer;
    sh:maxCount 1;
    sh:minCount 1;
    sh:path bgt:nauwkeurigheid.

  shape:nummeraanduidingreeks
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    rdfs:seeAlso "https://docs.geostandaarden.nl/imgeo/catalogus/bgt/#nummeraanduidingreeks"^^xsd:anyURI;
    sh:class bgt:Nummeraanduidingreeks;
    # [0-*]
    sh:path bgt:nummeraanduidingreeks.

  shape:object
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    rdfs:seeAlso "https://docs.geostandaarden.nl/nen3610/nldp/#provenance"^^xsd:anyURI;
    sh:class bgt:IMGeoObject;
    sh:maxCount 1;
    sh:minCount 1;
    sh:path foaf:primaryTopic.

  shape:objectBegintijd
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    rdfs:seeAlso "https://docs.geostandaarden.nl/imgeo/catalogus/bgt/#objectbegintijd"^^xsd:anyURI;
    sh:datatype xsd:date;
    sh:maxCount 1;
    sh:minCount 1;
    sh:name "Heeft object begintijd"@nl;
    sh:path nen3610:beginGeldigheid;
    skos:definition
      [ dct:creator "NEN 3610:2011";
        rdf:value "Datum waarop het object bij de bronhouder is ontstaan."@nl ];
    skos:scopeNote
      [ dct:source "https://docs.geostandaarden.nl/imgeo/catalogus/bgt/#levensduur-en-historie"^^xsd:anyURI;
        rdf:value """
<div lang="nl">
  <p>De formele levensduur wordt per object vastgelegd met de <code>shape:objectBegintijd</code> en <code>shape:objectEindtijd</code>, met als onderdeel daarvan de datum, die is bepaald door de bronhouder.</p>
</div>"""^^rdf:HTML ],
      [ dct:source "GEEN BRON";
        rdf:value "Deze datum moet altijd gelijk zijn aan de datum uit de tijdstipregistratie van het eerste voorkomen van het object."@nl ].

  shape:objectEindtijd
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    rdfs:seeAlso "https://docs.geostandaarden.nl/imgeo/catalogus/bgt/#objecteindtijd"^^xsd:anyURI;
    sh:datatype xsd:date;
    sh:maxCount 1;
    sh:minCount 0;
    sh:name "Heeft object eindtijd"@nl;
    sh:path nen3610:eindGeldigheid;
    skos:definition
      [ dct:creator "NEN 3610:2011";
        rdf:value "Datum waarop het object bij de bronhouder niet meer geldig is."@nl ];
    skos:scopeNote
      [ dct:source "https://docs.geostandaarden.nl/imgeo/catalogus/bgt/#levensduur-en-historie"^^xsd:anyURI;
        rdf:value """
<div lang="nl">
  <p>De formele levensduur wordt per object vastgelegd met de <code>shape:objectBegintijd</code> en <code>shape:objectEindtijd</code>, met als onderdeel daarvan de datum, die is bepaald door de bronhouder.</p>
</div>"""^^rdf:HTML ].

  shape:openbareRuimteNaam
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    rdfs:seeAlso "https://docs.geostandaarden.nl/imgeo/catalogus/bgt/#openbareruimtenaam"^^xsd:anyURI;
    sh:datatype xsd:string;
    sh:maxCount 1;
    sh:minCount 1;
    sh:path tbd:;
    skos:definition
      [ dct:creator "BAG";
        rdf:value "Een door het bevoegde gemeentelijke orgaan aan een OPENBARE RUIMTE toegekende benaming."@nl ];
    skos:scopeNote "Bij dit attribuut is aangegeven dat zowel materiële als formele historie worden bijgehouden. De openbare ruimtenaam is opgenomen in het BGT informatiemodel ten behoeve van visualisatie maar wordt overgenomen uit de BAG."@nl.

  shape:openbareRuimteType
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    rdfs:seeAlso "https://docs.geostandaarden.nl/imgeo/catalogus/bgt/#openbareruimtetype"^^xsd:anyURI;
    sh:class bgt:TypeOpenbareRuimte;
    sh:maxCount 1;
    sh:minCount 1;
    sh:path rdf:type;
    skos:definition
      [ dct:creator "BAG";
        rdf:value "De aard van de als zodanig benoemde OPENBARE RUIMTE."@nl ];
    skos:scopeNote "Bij dit attribuut is aangegeven dat zowel materiële als formele historie worden bijgehouden. De openbare ruimtenaam is opgenomen in het BGT informatiemodel ten behoeve van visualisatie maar wordt overgenomen uit de BAG."@nl.

  shape:plusFunctie
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:class owl:Class;
    sh:maxCount 1;
    sh:minCount 0;
    sh:path bgt:plusFunctie.

  shape:plusFysiekVoorkomen
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:class bgt:FysiekVoorkomen;
    sh:maxCount 1;
    sh:minCount 0;
    sh:path bgt:plusFysiekVoorkomen.

  shape:plusType
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:class owl:Class;
    sh:maxCount 1;
    sh:minCount 0;
    sh:path bgt:plusType.

  shape:publicatiedatumLandelijkeVoorzieningen
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:datatype xsd:dateTime;
    sh:maxCount 1;
    sh:minCount 0;
    sh:path bgt:publicatiedatumLandelijkeVoorzieningen.

  shape:registratie
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:class bgt:IMGeoObjectRegistratie;
    sh:maxCount 1;
    sh:minCount 1;
    sh:name "Heeft registratie"@nl;
    sh:node shape:GeoObjectRegistratie;
    sh:path foaf:isPrimaryTopicOf.

  shape:relatieveHoogteligging
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:datatype xsd:integer;
    sh:maxCount 1;
    sh:minCount 1;
    sh:path bgt:relatieveHoogteligging.

  shape:stadsdeel
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:class bgt:Stadsdeel;
    sh:maxCount 1;
    sh:minCount 0;
    sh:path bgt:stadsdeel.

  shape:status
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:class bgt:Status;
    sh:maxCount 1;
    sh:minCount 1;
    sh:path bgt:status.

  shape:talud
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:datatype xsd:boolean;
    sh:maxCount 1;
    sh:minCount 1;
    sh:path bgt:talud.

  shape:tekst
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    rdfs:seeAlso "https://docs.geostandaarden.nl/imgeo/catalogus/bgt/#tekst"^^xsd:anyURI;
    sh:datatype xsd:string;
    sh:maxCount 1;
    sh:minCount 1;
    sh:path bgt:tekst.

  shape:tijdstipRegistratie
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    rdfs:seeAlso
      "https://docs.geostandaarden.nl/imgeo/catalogus/bgt/#tijdstipregistratie"^^xsd:anyURI,
      "https://docs.geostandaarden.nl/nen3610/nldp/#regels-attributen-formeleHistorie"^^xsd:anyURI;
    sh:datatype xsd:dateTime;
    sh:maxCount 1;
    sh:minCount 1;
    sh:name "Heeft tijdstip registratie"@nl;
    sh:path prov:generatedAtTime;
    skos:definition
      [ dct:creator "NEN 3610:2011";
        rdf:value "Tijdstip waarop deze instantie van het object is opgenomen door de bronhouder."@nl ];
    skos:scopeNote """
<section lang="nl">
  <p>Als een mutatie niet resulteert in een nieuw object, dan ontstaat een nieuwe versie van het object.  In deze situatie verandert het <a href="https://bgt.basisregistraties.overheid.nl/bgt/def/tijdstipRegistratie">tijdstip registratie</a> van het object, terwijl de <a href="https://bgt.basisregistraties.overheid.nl/bgt/def/objectBeginTijd">object begintijd</a> gelijk blijft.</p>
</section>"""^^rdf:HTML.

  shape:type
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:class owl:Class;
    sh:maxCount 1;
    sh:minCount 0;
    sh:path bgt:type.

  shape:typeOverbruggingsdeel
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:class tbd:;
    sh:maxCount 1;
    sh:minCount 0;
    sh:path bgt:typeOverbruggingsdeel.

  shape:wijk
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:class bgt:Wijk;
    sh:maxCount 1;
    sh:minCount 0;
    sh:path bgt:wijk.

  shape:wijkcode
    a sh:PropertyShape;
    rdfs:isDefinedBy graph:shape;
    sh:datatype xsd:string;
    sh:maxCount 1;
    sh:minCount 1;
    sh:path bgt:wijkcode.
}
